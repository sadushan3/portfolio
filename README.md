
  # Use Photo in Prompt

  This is a code bundle for Use Photo in Prompt. The original project is available at https://www.figma.com/design/qw476SrpgQQV8BF642SCNX/Use-Photo-in-Prompt.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  